using System;
using System.IO;
using System.Reflection;

namespace BlockBase.Dapps.CloudManager.DataAccessLayer
{
    class Program
    {
        static void Main(string[] args)
        {
            /*NodeDAO c = new NodeDAO();
            var output =  c.ListAll();
             foreach (var node in output.Result) 
             {
                 Console.WriteLine(node.Account);
             }
            c.Insert(new Data.Node {Account="Provider2", Service= "Azure", Status ="OFF", Type="Provider" });
            Console.WriteLine("-----------------------------------------------------");
            output = c.ListAll();
            foreach (var node in output.Result)
            {
                Console.WriteLine(node.Account);
            }*/

        }
    }
}
